# PostgreSQL connection (local dev via docker-compose)
DATABASE_URL=postgresql://shoperator:shoperator@localhost:5432/shoperator

# Admin API token — set a long random string
ADMIN_TOKEN=change-me-to-something-secret

# Port for the API server
PORT=3001

# CORS origin for the web app
CORS_ORIGIN=http://localhost:5173
